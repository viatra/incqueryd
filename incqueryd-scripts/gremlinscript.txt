conf = new BaseConfiguration();
conf.setProperty("storage.directory", "graphdir");
conf.setProperty("storage.backend", "berkeleyje");
conf.setProperty("storage.batch-loading", "true");
// increase from 10000
conf.setProperty("ids.block-size", 100000);
g = TitanFactory.open(conf);
g.createKeyIndex("idx", Vertex.class);
g.createKeyIndex("type", Vertex.class);

b = new BatchGraph(g);
b.loadGraphML('/home/szarnyasg/temp/models/original/testBig_User_SIZE.graphml');
