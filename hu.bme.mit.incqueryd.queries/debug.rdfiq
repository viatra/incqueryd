vocabulary <railway.rdf>

base <http://www.semanticweb.org/ontologies/2011/1/TrainRequirementOntology.owl#>

pattern routeSensor(Sen) = {
	Sensor(Sen);
	neg find head(Sen, _);
}

pattern head(Sen, R) = {
	Route_routeDefinition(R, Sen);
}